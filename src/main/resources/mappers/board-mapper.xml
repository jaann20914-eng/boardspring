<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="Board">

	<select id="selectAll" resultType="com.keud.boardproject.dto.BoardDTO">
		select * from boards
	</select>
	
	<select id="searchBuTitle" resultType="com.keud.boardproject.dto.BoardDTO">
		select * from baords where title like '%'||#{title}||'%'
	</select>
	
	<insert id="Boardinsert">
		insert into boards values(boards_seq.nextval,#{writer},#{title},#{contents},#{writer_date}, #{view_count});
	</insert>

</mapper>